# Dependencies (everyone re-downloads these via npm install — never commit)
node_modules/

# Environment variables (secrets like database passwords, JWT secret keys)
.env

# OS-generated files
.DS_Store
Thumbs.db
